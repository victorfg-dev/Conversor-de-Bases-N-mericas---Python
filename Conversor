# Conversor de bases númericas.
# Estrutura de entrada de dados
num = int(input('Qual número você deseja converter? '))
basei = int(input('Qual base este número está? '))
basef = int(input('Para qual base? '))

# Conversão basei --> base 10
num1 = str(num)
expo = len(num1)
alg = 0

soma = 0
while expo != 0:
    soma += ((basei ** (expo-1))* int(num1[alg]))
    alg += 1
    expo -= 1
print(soma)
